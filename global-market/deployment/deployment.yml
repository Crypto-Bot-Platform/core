apiVersion: apps/v1
kind: Deployment
metadata:
  name: global-market
  labels:
    app: global-market
spec:
  selector:
    matchLabels:
      octopusexport: OctopusExport
  replicas: 1
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: global-market
        octopusexport: OctopusExport
    spec:
      hostNetwork: true
      containers:
        - name: global-market
          image: 'tsboris/cbp-global-market:latest'
